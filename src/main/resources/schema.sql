CREATE TABLE IF NOT EXISTS TB_NAVIS_VISIT_DETAILS (ID LONG PRIMARY KEY,  CARGO_TYPE VARCHAR(50), DRIVER_NAME VARCHAR(50), GATE_IN DATE, GATE_OUT DATE, ID_DRIVER VARCHAR(50), ID_TRUCK VARCHAR(50), WEIGHT FLOAT );